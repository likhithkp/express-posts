<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f7fc;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .profile-container {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 700px;
            transition: all 0.3s ease;
        }

        .profile-container:hover {
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
        }

        .profile-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #f0f0f0;
            padding-bottom: 10px;
        }

        .profile-header h1 {
            margin: 0;
            font-size: 26px;
            color: #333;
            font-weight: 600;
        }

        .logout-btn {
            background-color: #e74c3c;
            color: white;
            border: none;
            padding: 12px 18px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 14px;
            text-decoration: none;
            transition: background-color 0.3s ease;
        }

        .logout-btn:hover {
            background-color: #c0392b;
        }

        .post-form {
            margin-bottom: 30px;
        }

        .post-form textarea {
            width: 100%;
            padding: 14px;
            font-size: 16px;
            border: 1px solid #ddd;
            border-radius: 10px;
            resize: vertical;
            box-sizing: border-box;
            outline: none;
            transition: border-color 0.3s ease;
        }

        .post-form textarea:focus {
            border-color: #3498db;
        }

        .post-form button {
            background-color: #3498db;
            color: white;
            border: none;
            padding: 12px 18px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 16px;
            width: 100%;
            margin-top: 15px;
            transition: background-color 0.3s ease;
        }

        .post-form button:hover {
            background-color: #2980b9;
        }

        .posts-section {
            margin-top: 40px;
        }

        .posts-section h2 {
            font-size: 22px;
            color: #333;
            margin-bottom: 20px;
            border-bottom: 3px solid #3498db;
            padding-bottom: 10px;
        }

        .posts-list {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }

        .posts-list li {
            background-color: #ffffff;
            border: 1px solid #e4e4e4;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 20px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .posts-list li:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
        }

        .post-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }

        .post-username {
            font-weight: 600;
            font-size: 16px;
            color: #2c3e50;
        }

        .post-content {
            font-size: 16px;
            color: #555;
            line-height: 1.6;
            margin-bottom: 15px;
        }

        .post-actions {
            display: flex;
            justify-content: flex-end;
            gap: 15px;
            margin-top: 15px;
        }

        .like-section,
        .edit-section {
            display: flex;
            align-items: center;
        }

        .like-btn,
        .edit-btn {
            background-color: #16a085;
            color: white;
            border: none;
            padding: 8px 14px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 14px;
            transition: background-color 0.3s ease;
        }

        .like-btn {
            background-color: #2ecc71;
        }

        .edit-btn {
            background-color: #f39c12;
        }

        .like-btn:hover,
        .edit-btn:hover {
            opacity: 0.8;
        }

        .post-date {
            margin-top: 10px;
            font-size: 14px;
            color: #888;
        }

        .post-date small {
            font-style: italic;
        }
    </style>
</head>

<body>
    <div class="profile-container">
        <form action="/update/<%= post._id %>" method="post" class="post-form">
            <textarea name="content" rows="4" placeholder="Write your post here..."><%= post?.content %></textarea>
            <button type="submit">Update</button>
        </form>
    </div>
</body>

</html>